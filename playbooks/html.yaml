---
- hosts: all
  become: true
  tasks:
  - name: Installing httpd packages
    yum:
      name: httpd
      state: present

  - name:
    service:
       name: httpd
       state: started
       enabled: true

